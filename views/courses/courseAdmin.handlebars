<header>
    <h1> {{title}}</h1>
</header>

<div>
    <ol>
        {{#each allCourses}}
        <div>
            <li>
                <h3>{{courseTitle}}</h3>
                <p>{{courseId}}</p>
                <p>{{description}}</p>
                <p>{{professorId}}</p>
                <p>{{professorName}}</p>
            </li>
        </div>
        {{/each}}
    </ol>
</div>